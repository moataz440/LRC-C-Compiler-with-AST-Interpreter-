#include <iostream>
using namespace std;

int main() {
  int x = 0, y = 2;
  int arr[3] = {1, 2, 3};

  if (y > 1) {
    x = x + arr[1];
  } else {
    x = x + 10;
  }

  for (int i = 0; i < 3; i = i + 1) {
    x = x + arr[i];
  }
cout<<x;
  return 0;
}

